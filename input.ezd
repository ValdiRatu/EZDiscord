Token='123'
ClientID = '1234'
GuildID=['1234', '1234']

var user = 'Joe'
var test = concat(user, 2, true)

command addition(num1: number, num2: number) {
    var test = [1,2,3,4,5,6]
    var double = []
    var message = '' 
    for (apple in test) {
        var two = apple*2
        message = concat(message, two, '\n')
        add(double, two)
    }
    reply(message)
}